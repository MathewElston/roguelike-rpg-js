const satCollision = (a,b) => {

    // project perpindicular vector.normalize() of each side of the polygon to find an axis
     // loop all the verticies
    // if there is a gap, not colliding
    // get min and max of each polygon coordinate
    //if we have an overlapping axis, move to the next vector axist, the perpendicular vectors
    // else if there is a sepearting axis, then there is no collision
   
}