const input = (game) => {
  // RIGHT
  if (keyState.value & 1) {
  }

  // LEFT
  if (keyState.value & 2) {
  }

  // UP
  if (keyState.value & 4) {
  }
  // DOWN
  if (keyState.value & 8) {

  }

  // SPACE
  if (keyState.value & 16) {
  }
};
